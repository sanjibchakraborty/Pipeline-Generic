# frozen_string_literal: true

# xcodeproj (fastlane) requires CFPropertyList < 4; CFPropertyList 3.x requires Ruby < 3.2. Use Ruby 3.1.x locally and in CI.
source "https://rubygems.org"

gem "fastlane", "~> 2.225"
